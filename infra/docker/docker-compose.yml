version: "3.8"
services:
  api:
    build: ../api
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    volumes:
      - ../api:/app
    command: npm run dev
  frontend:
    build: ../frontend
    ports:
      - "5173:5173"
    volumes:
      - ../frontend:/app
    command: npm run dev
